language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t jeyfarmer/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jeyfarmer/docker-react npm run test